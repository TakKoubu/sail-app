<%= form_with(url: sale_sale_comments_path(@sale), model: @sale_comment ,local: true) do |form| %>
  <% if sale_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_comment.errors.count, "error") %> prohibited this sale_comment from being saved:</h2>

      <ul>
      <% sale_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, name: "sale_comment[content]" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
